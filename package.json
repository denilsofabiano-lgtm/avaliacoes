{
  "name": "sistema-avaliacoes-fullstack",
  "version": "1.0.0",
  "description": "Sistema de Avaliações - Frontend Angular + Backend Symfony",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "frontend": "cd sistema-avaliacoes && npm start",
    "backend": "cd sistema-avaliacoes-api && php -S localhost:8081 -t public",
    "backend:setup": "cd sistema-avaliacoes-api && composer install && mkdir -p var && php bin/console doctrine:migrations:migrate --no-interaction",
    "backend:setup-mysql": "cd sistema-avaliacoes-api && composer install && php bin/console doctrine:database:create --if-not-exists && php bin/console doctrine:migrations:diff --no-interaction && php bin/console doctrine:migrations:migrate --no-interaction",
    "backend:install-docs": "cd sistema-avaliacoes-api && composer update nelmio/api-doc-bundle",
    "backend:init": "cd sistema-avaliacoes-api && composer install && mkdir -p var && php bin/console doctrine:schema:create",
    "backend:fixtures": "cd sistema-avaliacoes-api && php bin/console doctrine:fixtures:load --no-interaction",
    "backend:jwt": "cd sistema-avaliacoes-api && mkdir -p config/jwt && php bin/console lexik:jwt:generate-keypair --skip-if-exists",
    "install-all": "cd sistema-avaliacoes && npm install && cd ../sistema-avaliacoes-api && composer install",
    "setup": "npm run install-all && npm run backend:setup",
    "setup-mysql": "npm run install-all && npm run backend:setup-mysql",
    "setup-full": "npm run setup && npm run backend:fixtures && npm run backend:jwt",
    "dev": "npm run start",
    "serve-static": "npx serve . -p 4200"
  },
  "devDependencies": {
    "serve": "^14.2.1",
    "concurrently": "^8.2.2"
  },
  "keywords": ["angular", "symfony", "typescript", "php", "fullstack"],
  "author": "Sistema Avaliacoes Team",
  "license": "MIT"
}
